# ======================
# Service URLs
# ======================

# Dashboard (Frontend) URL - where the React app is served (via nginx proxy)
DASHBOARD_URL=http://localhost

# Frontend Port - Vite dev server port (internal, proxied by nginx)
FRONTEND_PORT=3000

# Backend (External API) Port - REST API for frontend
BACKEND_PORT=5000

# Orchestrator (Internal API) URL - where backend connects to orchestrator
ORCHESTRATOR_URL=http://localhost:8000

# Orchestrator Port - internal API port
ORCHESTRATOR_PORT=8000

# ======================
# Security
# ======================

# Orchestrator Security Token
# Generate a strong random token for production: openssl rand -hex 32
# Leave empty for development on localhost (authentication disabled)
ORCHESTRATOR_SECRET=

# ======================
# CORS Configuration
# ======================

# Allowed origin for CORS (frontend URL that can call the backend)
# This should match DASHBOARD_URL since users access via nginx on port 80
CORS_ORIGIN=http://localhost
